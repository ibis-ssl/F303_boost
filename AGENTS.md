# 回答

1. ユーザーへの最終回答は日本語で記述する。
2. テキストファイルはUTF-8で扱い、日本語の文字化けを避ける。

# プロジェクト

1. 本プロジェクトはSTM32F303CBT6で昇圧回路、キッカー、電源監視、ADNS3080、およびCAN通信を制御するファームウェアである。
2. 実行環境は特定のSTM32基板に限定される。組み込みCとして実装し、C++固有機能を混在させない。
3. メインループ、CAN割り込み、保護処理では実行時間と決定性を重視する。処理量を増やす変更では周期への影響を確認する。
4. 高電圧・大電流を扱うため、保護判定、PWM停止、電源出力無効化、IWDGを弱める変更は行わない。必要な場合は理由、故障時の挙動、検証方法を明示する。
5. 実機への書き込みや出力動作は、ユーザーから明示的に依頼された場合だけ行う。

# STM32CubeMX / HAL

1. `orion_F303_boost_v3.ioc`を周辺機能設定の基準とする。
2. CubeMX生成ファイルを編集する場合は、原則として`USER CODE BEGIN`と`USER CODE END`の範囲へ変更を入れ、再生成で失われないようにする。
3. ピン、クロック、DMA、割り込み、ADC、PWM、CAN設定を変更した場合は、`.ioc`と生成コードの整合を確認する。
4. 新規のCソース／ヘッダには、ファイルの目的と責務が分かるコメントを冒頭に記載する。

# テスト

1. Cコード、ビルド設定、リンカ設定を変更した場合は、少なくともDebugビルドが成功するまで確認する。
2. PowerShellスクリプトを変更した場合は、構文チェックを行う。ビルド関連スクリプトでは可能なら実際のDebugビルドも確認する。
3. 標準のビルドコマンドは次とする。

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File .\Script\build.ps1 -Configuration Debug
```

4. Flash、CAN送信、キック、昇圧など実機状態を変える試験は自動実行しない。実行した場合は条件と結果を報告する。

# ドキュメント

1. ドキュメントは日本語で記述する。
2. 重要な設計、制御フロー、CANプロトコル、保護仕様の変更は`doc/overview.md`へ反映する。
3. MCU、クロック、ピン、周辺機能、計測対象などの変更は`doc/hardware_spec.md`へ反映する。
4. 開発環境、基本操作、スクリプトの利用方法が変わった場合は`README.md`を更新する。
